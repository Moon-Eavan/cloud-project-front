# 1. 기본 개념

## 1.1 Calendar(캘린더)

- **사용 위치**: 메인 캘린더 화면 좌측 사이드바 및 일정 표시의 상위 그룹

- **종류**
  - `Google Calendar`: 사용자가 OAuth로 연동한 Google 캘린더
  - `Calendar`: 서비스 자체에서 관리하는 로컬 캘린더
  - `E-Campus`: e-campus 과제 정보를 일정 형태로 보여주는 **읽기 전용** 캘린더  
    - My Page에서 사용자가 등록한 **E-Campus 토큰**을 사용해 과제 정보를 동기화한다.

- **필드(예시)**
  - `id`: 고유 식별자
  - `name`: 캘린더 이름 (예: "Google Calendar", "Calendar", "E-Campus")
  - `type`: `enum(google, local, ecampus)`
  - `color`: 캘린더를 구분하기 위한 색상
  - `isVisible`: boolean (사이드바에서 체크 여부)

- **동작**
  - 좌측 사이드바에 각 캘린더가 리스트로 표시된다.
  - 사용자는 각 캘린더 옆의 체크박스를 통해 `isVisible`을 on/off 할 수 있다.
  - `isVisible == true` 인 캘린더에 속한 `schedule`만 메인 캘린더에 렌더링된다.
  - 체크 해제 시, 해당 캘린더의 모든 `schedule`은 화면에서 **숨기기만** 하고 데이터는 삭제하지 않는다.

- **Schedule과의 관계**
  - 각 `schedule`은 어떤 캘린더에 속하는지 나타내기 위해 `calendarId`를 가진다.
  - `calendarId`는 위에서 정의한 `Calendar`의 `id`를 참조한다.
  - 사용자가 수동으로 `schedule`을 추가할 때 선택할 수 있는 캘린더는  
    `Google Calendar`, `Calendar` 두 가지이며,  
    `E-Campus`는 동기화 전용(read-only)이라 수동 선택이 불가능하다.

---

## 1.2 Schedule(캘린더 일정)

- **사용 위치**: 메인 캘린더  

- **필드**
  - `title`: 문자열 (필수)
  - `description`: 문자열 (선택)
  - `start`: 날짜+시간
  - `end`: 날짜+시간
  - `location`: 문자열
  - `isCompleted`: boolean  
    - `true`일 경우 캘린더에서 **취소선**으로 표시
  - `calendarId`: 이 일정이 속한 캘린더 ID  
    - `google/local/ecampus` 타입의 `Calendar` 중 하나를 참조

---

## 1.3 Task(작업)

- **사용 위치**: 칸반보드(Kanban), Gantt 차트  

- **필드**
  - `title`: 문자열 (필수)
  - `description`: 문자열 (선택)
  - `startDate`: 날짜
  - `endDate`: 날짜
  - `status`: `enum(todo, progress, done)`
  - `parentTaskId`: 상위 태스크 ID (없으면 `null` = parent task)

- **용어**
  - **parent task**: `parentTaskId == null` 인 태스크  
  - **subtask**: `parentTaskId != null` 인 태스크  

> **중요**: 캘린더의 `schedule`과 칸반/간트의 `task`는 서로 다른 개념이다.  
> 단, 캘린더에서 특정 기능을 이용할 때 `task`를 새로 생성할 수 있다.

---

# 2. 회원 & 로그인 / 외부 서비스 연동 플로우

## 2.1 회원가입 & 로그인

1. 사용자는 **서비스 전용 이메일 / 비밀번호**로 회원가입한다.
   - 이메일 형식 검증을 수행한다.
   - 비밀번호는 해시 등 보안 방식으로 저장한다.
2. 회원가입 이후, 사용자는 등록한 **이메일 / 비밀번호**로 로그인한다.
3. 로그인에 성공하면 **메인 화면(캘린더 화면)** 으로 이동한다.
   - 메인 화면에는 캘린더와 좌측 사이드바(캘린더 리스트, 알림 영역 등)가 표시된다.

> E-Campus 계정 정보(ID/PW)는 회원가입/로그인에 사용하지 않는다.  
> E-Campus 연동은 별도의 **토큰 기반 연결**로 처리한다.

---

## 2.2 Google Calendar 연동 (마이페이지) 백엔드에서 미구현!!

1. 사용자는 **마이페이지**에서 “Google 계정 연결” 기능을 사용할 수 있다.
2. Google OAuth 플로우를 통해 사용자가 권한을 허용하면,
   - 서비스는 Google 액세스 토큰/리프레시 토큰을 안전하게 저장한다.
3. 연동 완료 후:
   - Google Calendar의 일정이 `Google Calendar` 타입의 `Calendar`와 연동된 `schedule`로 가져와진다.
   - 메인 캘린더에서 사이드바의 `Google Calendar` 표시 여부에 따라 보이거나 숨겨진다.
4. 사용자가 캘린더에서 새 일정을 추가할 때, **캘린더 선택 옵션**에서  
   `Google Calendar`를 선택하면 해당 일정은 Google API를 통해 실제 Google Calendar에도 반영된다.

---

## 2.3 E-Campus 연동 (마이페이지, 토큰 기반)

1. 사용자는 **마이페이지**에서 “E-Campus 연동” 영역에 접근한다.
2. 사용자는 E-Campus에서 발급받은 **토큰**을 입력하거나,  
   (향후 가능하다면) E-Campus 연동 버튼을 통해 토큰을 자동으로 받아 올 수 있다.
3. 사용자가 유효한 토큰을 저장하면:
   - 서비스는 해당 토큰을 이용해 E-Campus API(또는 스크래핑 서버 등)에 접근해 과제 정보를 조회한다.
   - 조회된 과제는 `E-Campus` 타입 `Calendar`에 속한 `schedule`로 변환되어 캘린더에 표시된다.
   - 과제 마감 기한에 맞춰 `start/end`를 설정하며, 제출 완료된 과제는 `isCompleted = true`로 처리하고 **취소선**으로 표시한다.
4. E-Campus 캘린더는 읽기 전용이며, 사용자가 새 일정을 직접 추가하는 것은 불가능하다.
   - 사용자가 사이드바에서 `E-Campus` 캘린더 표시 체크를 끄면, 해당 과제 일정들은 화면에서 숨겨진다.

---

# 3. Task–Kanban–Gantt 동기화 규칙

## 3.1 Gantt 차트에서 Task 생성 규칙

### 상황 1: Gantt에서 parent task만 생성한 경우

- **조건**
  - Gantt 차트에서 `parent task`를 생성한다.
  - 이 `parent task`는 아직 `subtask`가 없다.

- **결과**
  - Gantt에 `parent task` 생성
  - 칸반보드의 `todo` 컬럼에 **같은 parent task** 생성

---

### 상황 2: Gantt에서 subtask를 생성한 경우

- **조건**
  - Gantt 차트에서 어떤 `parent task`에 `subtask`를 생성한다.

- **결과**
  - Gantt에 `subtask` 생성
  - 칸반보드 `todo` 컬럼:
    - 해당 `parent task`가 `todo`에 있었다면 **parent task를 제거**
    - 새로 생성된 **모든 subtask**를 `todo` 상태로 추가

**정리**

- Gantt에서 작업 생성 시  
  - “subtask 없는 parent task” → Kanban `todo`에 parent task 1개  
  - “subtask를 가진 parent task” → Kanban `todo`에는 parent는 제거, 대신 **모든 subtask가 `todo`**

---

## 3.2 칸반보드에서 Task 생성 규칙

- **조건**
  - 칸반보드에서 새로운 작업을 생성한다.
  - 칸반보드에서는 **parent task만 생성 가능**하다. (subtask 직접 생성 X)

- **결과**
  - 칸반보드 `todo` 컬럼에 **parent task** 생성
  - Gantt 차트에도 **subtask가 없는 parent task**를 동일하게 생성

**즉,**

- **Kanban → Gantt**: parent task 동시 생성  
- **Gantt → Kanban**: parent / subtask 상태에 따라 `todo`에 동기화

---

## 3.3 Task 상태 변경과 Done 동기화

### 상황 1: 칸반보드에서 상태 변경

- **조건**
  - 사용자가 칸반보드에서 task를  
    `todo → progress` 또는 `progress → done` 으로 이동한다.

- **결과**
  - 해당 task의 `status` 값을 변경한다.
  - `status == done`이 된 task는:
    - Gantt 차트에서 **취소선**으로 표시한다.

---

### 상황 2: Gantt 차트에서 완료 처리

- **조건**
  - Gantt 차트에서 어떤 task를 “완료(done)” 상태로 지정한다.

- **결과**
  - 해당 task의 `status`를 `done`으로 변경
  - Gantt 차트에서는 해당 task를 **취소선**으로 표시
  - 칸반보드에서도 이 task를 `done` 컬럼으로 이동

---

# 4. 캘린더 Schedule → Task 생성 규칙

## 4.1 Schedule과 Task의 구분

- `schedule`은 캘린더에 표시되는 **시간 기반 일정** (날짜+시간)
- `task`는 할 일/작업 단위로, **칸반보드/Gantt**에서 관리
- 서로 다른 엔티티이지만, 일정에서 **“task에 추가”** 기능을 통해 연결될 수 있음

---

## 4.2 “task에 추가” 버튼 동작

- **조건**
  - 사용자가 캘린더에서 특정 `schedule`에 대해 **“task에 추가”** 버튼을 누른다.

- **결과**
  - 새로운 **parent task**를 생성한다.
  - 이 task는 **칸반보드와 Gantt 차트에 동시에 생성**된다.
  - 생성되는 task의 필드는 다음과 같이 매핑한다.
    - `title` = schedule의 `title`
    - `description` = schedule의 `description`
    - `startDate` = 오늘 날짜(현재 날짜)
    - `endDate` = 해당 schedule의 날짜 (필요 시 schedule의 `end` 기준)
    - `status` = `todo`
    - `parentTaskId` = `null` (parent task)

---

# 5. 친구 및 그룹 기능

## 5.1 친구 기능

- 사용자 A는 **상대방의 ID**로 친구 추가를 할 수 있다.
- 친구 추가에 성공하면:
  - A의 **친구 목록**에 해당 사용자가 표시된다.
- 친구 목록을 통해 자신이 추가한 친구들을 확인할 수 있다.

---

## 5.2 그룹(Group) 기능

- **목적**: 프로젝트, 조별 과제, 스터디 등 **공동 일정 관리**

- **그룹 생성 규칙**
  - 사용자는 **이미 친구로 추가된 사용자들만** 그룹 멤버로 선택할 수 있다.
  - 선택한 친구들 + 자신을 포함하여 그룹을 생성한다.

---

# 6. 그룹 탭 내 일정 기능

그룹 탭에는 두 가지 일정 기능이 있다.

1. **일정 추가**
2. **일정 조율**

---

## 6.1 일정 추가

- **의미**: 그룹원들끼리 **이미 합의가 끝난 일정**을 등록하는 기능

- **입력 항목**
  - `title`: 일정 제목
  - `description`: 내용 (선택)
  - `location`: 장소 (선택)
  - `start`: 시작 일시 (날짜+시간)
  - `end`: 종료 일시 (날짜+시간)
  - `members`: 참여 멤버 목록

- **결과**
  - 선택된 멤버 각각의 캘린더에 이 일정이 `schedule`로 생성된다.
  - 각 멤버에게 **알림(Notification)**을 발송한다.
  - 그룹 탭에서도 해당 일정을 조회할 수 있으며,  
    어떤 멤버들이 참여하는지 확인 가능하다.

---

## 6.2 일정 조율

### 6.2.1 입력 단계

- 사용자는 다음 정보를 선택한다.
  - `members`: 참여 대상 멤버들
  - `period`: 일정 후보 기간 (예: `2025-11-20 ~ 2025-11-30`)

---

### 6.2.2 표시 규칙

- 선택한 `period` 동안, 각 멤버의 일정 정보를 바탕으로 그룹 조율 화면에 시간대를 표시한다.

- **프라이버시 규칙**
  - **다른 사람의 일정**
    - 해당 시간대를 **회색 블록**으로만 표시
    - 일정의 제목/내용 등 구체 정보는 표시하지 않는다.
  - **내 일정**
    - 나의 캘린더 일정은 회색 블록 +  
      그 시간에 어떤 일정이 있는지 **제목**을 함께 표시한다.

> 목적: 남의 일정 내용은 보호하면서, 본인은 자신의 일정 내용을 보며 일정을 잡을 수 있게 한다.

---

### 6.2.3 시간 선택 및 일정 생성

- 사용자는 조율 화면에서 **회색이 아닌 빈 시간대**를 선택한다.

- **조건**
  - 빈 시간대를 선택한 후 “다음” 또는 유사 버튼을 눌러 **일정 추가 화면으로 이동**한다.

- **결과 (일정 추가 화면 초기값)**
  - `start`, `end`: 방금 사용자가 선택한 시간
  - `members`: 조율에 선택했던 멤버들

- 사용자는 `title`, `description`, `location` 등을 입력/수정 후 일정을 생성한다.

- **일정 생성 결과**
  - 선택된 멤버들의 캘린더에 모두 `schedule`로 등록된다.
  - 각 멤버에게 알림이 발송된다.
  - 그룹 탭에서도 해당 일정을 확인할 수 있고,  
    참여 멤버 목록을 볼 수 있다.

---

# 7. 알림(Notification) 기능

## 7.1 알림 UI

- 알림창은 메인 화면 **좌측 하단**에 위치한다.
- 최근 발생한 이벤트를 리스트 형태로 표시하며, 보통 최신순 정렬을 사용한다.
- 각 알림은 관련 화면으로 이동하는 액션(예: 친구 목록, 그룹 탭, 캘린더 상세 등)을 포함할 수 있다.

---

## 7.2 알림 발생 트리거

현재 구현된 주요 알림 트리거는 다음과 같다.

1. **친구 요청 수신**
   - 다른 사용자가 나에게 친구 요청을 보냈을 때 알림 생성
   - 내용 예시: 요청자 이름, 수락/거절 버튼 또는 해당 화면으로 이동하는 링크

2. **그룹 일정 조율 후 캘린더에 일정 등록**
   - 일정 조율 플로우(6.2)를 통해 시간이 확정되고,  
     실제 `schedule`이 각 멤버의 캘린더에 생성될 때 알림 생성
   - 해당 알림을 클릭하면 관련 그룹 또는 해당 일정이 보이는 캘린더 화면으로 이동

3. **기타 이벤트 확장 가능**
   - 위 두 가지 외에, 동일한 패턴으로 필요한 이벤트(예: 그룹 일정 직접 추가, 초대 수락 등)에 대해  
     알림을 확장할 수 있도록 설계한다.

알림은 서비스 내에서 발생하는 주요 상태 변화를 사용자에게 알려 주는 공통 메커니즘이며,
알림 데이터는 "어떤 타입의 이벤트인지"와 "어떤 리소스(친구/그룹/일정 등)와 연결되는지"를 포함해야 한다.

---

# 8. 백엔드 아키텍처

> **백엔드 위치**: `/cloud-project/UniSync/` (UniSync-front와 동일 레벨)

백엔드는 마이크로서비스 아키텍처로 구성되어 있으며, 상세한 설계 문서는 백엔드 저장소에서 관리됩니다.

---

## 8.1 주요 문서 링크

백엔드 아키텍처 및 설계에 대한 상세 내용은 다음 문서를 참고하세요:

### 📚 필수 문서

1. **[../UniSync/README.md](../UniSync/README.md)**
   - 프로젝트 전체 개요
   - 빠른 시작 가이드
   - 기술 스택 및 아키텍처 다이어그램

2. **[../UniSync/app/backend/README.md](../UniSync/app/backend/README.md)**
   - 백엔드 서비스 실행 방법
   - 환경변수 설정
   - API Gateway 라우팅 규칙
   - DDD 패키지 구조

3. **[../UniSync/docs/design/system-architecture.md](../UniSync/docs/design/system-architecture.md)**
   - 시스템 아키텍처 상세 설계
   - 데이터베이스 스키마
   - API 설계
   - 인증/보안

4. **[../UniSync/docs/design/sqs-architecture.md](../UniSync/docs/design/sqs-architecture.md)**
   - SQS 큐 목록 및 메시지 스키마
   - 비동기 이벤트 처리 흐름
   - 재시도 전략

5. **[../UniSync/docs/features/canvas-sync.md](../UniSync/docs/features/canvas-sync.md)**
   - Canvas 동기화 상세 설계
   - Phase 1/2/3 로드맵

---

## 8.2 빠른 참조 - 서비스 구성

### API Gateway (:8080)
- JWT 인증/인가 (AWS Cognito)
- 모든 백엔드 서비스 라우팅
- Swagger API 통합

### User-Service (:8081)
- 사용자 인증 (회원가입/로그인)
- Canvas/Google 토큰 관리 (암호화)
- 친구 관리
- 그룹 관리

### Course-Service (:8082)
- Canvas 과목/과제 관리
- SQS 메시지 처리 (Canvas 동기화)
- 과제 → 일정 변환

### Schedule-Service (:8083)
- 시간 기반 일정(Schedule) 관리
- 기간 기반 할일(Todo) 관리
- 캘린더 카테고리 관리

### Serverless
- **Canvas-Sync-Lambda**: Canvas API 동기화 (Python)
- **Google-Calendar-Sync**: Google Calendar 연동 (계획)

---

## 8.3 프론트엔드와 백엔드 매핑

### API 엔드포인트 매핑

| 프론트엔드 개념 (spec.md 1~7장) | 백엔드 서비스 | API 엔드포인트 | 비고 |
|--------------------------|------------|--------------|------|
| **Calendar** (google/local/ecampus) | Schedule-Service | `/api/v1/categories` | 캘린더 타입 관리 |
| **Schedule** (캘린더 일정) | Schedule-Service | `/api/v1/schedules` | 시간 단위 일정 |
| **Task** (칸반/간트) | Schedule-Service | `/api/v1/todos` | 기간 단위 할일 |
| **Canvas 과제** | Course-Service | `/api/v1/assignments` | E-Campus 연동 |
| **친구** | User-Service | `/api/v1/friends` | 친구 관계 관리 |
| **그룹** | User-Service | `/api/v1/groups` | 그룹 및 멤버 관리 |
| **알림** | User-Service | `/api/v1/notifications` | 알림 발송/조회 |

> **참고**: 클라이언트는 모든 요청을 `/api` prefix와 함께 API Gateway로 전송합니다.
> Gateway는 `/api`를 제거하고 해당 백엔드 서비스로 라우팅합니다.

---

## 8.4 주요 데이터 흐름

### Canvas 과제 동기화 (spec.md 1.3, 2.3과 연동)

```
[사용자가 Canvas 동기화 버튼 클릭]
    ↓
[프론트엔드] POST /api/v1/sync/canvas
    ↓
[API Gateway] JWT 인증 → User-Service
    ↓
[User-Service] Canvas 토큰 조회 → Canvas-Sync-Lambda 호출
    ↓
[Lambda] Canvas API 호출 → 과제 데이터 조회 → SQS 발행
    ↓
[Course-Service] SQS 구독 → Assignment 저장 → Schedule 변환 이벤트 발행
    ↓
[Schedule-Service] Schedule 생성 (E-Campus 캘린더에 자동 추가)
    ↓
[프론트엔드] 캘린더 화면에서 E-Campus 일정 표시
```

**매핑 규칙**:
- Canvas `assignment.title` → Schedule `title`
- Canvas `assignment.due_date` → Schedule `end`
- 모든 Canvas 과제는 `E-Campus` 타입 Calendar에 속함 (spec.md 1.1 참고)
- 제출 완료된 과제는 `isCompleted = true`로 표시 (취소선)

---

### 그룹 일정 조율 (spec.md 6.2와 연동)

```
[사용자가 그룹에서 일정 조율 시작]
    ↓
[프론트엔드] POST /api/v1/groups/{groupId}/schedules/coordinate
    ↓
[User-Service] 그룹 멤버 조회 및 권한 확인
    ↓
[Schedule-Service] 각 멤버의 일정 조회
    ↓
[프론트엔드] 조율 화면에 표시
    - 다른 사람 일정: 회색 블록 (제목 숨김)
    - 내 일정: 회색 블록 + 제목
    ↓
[사용자가 빈 시간 선택 후 일정 생성]
    ↓
[프론트엔드] POST /api/v1/schedules (members 포함)
    ↓
[Schedule-Service] 각 멤버의 캘린더에 일정 추가
    ↓
[User-Service] 각 멤버에게 알림 발송
```

---

## 8.5 로컬 개발 환경 실행

```bash
# 1. 백엔드 저장소로 이동
cd ../UniSync

# 2. 인프라 시작 (MySQL, LocalStack)
docker-compose up -d

# 3. 각 서비스 실행 (터미널 4개 사용)
# Terminal 1 - API Gateway
cd app/backend/api-gateway && ./gradlew bootRun

# Terminal 2 - User Service
cd app/backend/user-service && ./gradlew bootRun

# Terminal 3 - Course Service
cd app/backend/course-service && ./gradlew bootRun

# Terminal 4 - Schedule Service
cd app/backend/schedule-service && ./gradlew bootRun
```

**서비스 접속**:
- API Gateway: http://localhost:8080
- 통합 Swagger: http://localhost:8080/swagger-ui.html

**상세 설정**: [../UniSync/app/backend/README.md](../UniSync/app/backend/README.md) 참고

---

## 8.6 개발 시 참고사항

### 프론트엔드 개발 시

1. **인증**: 모든 API 요청에 `Authorization: Bearer {token}` 헤더 필요
   - 회원가입/로그인 응답에서 받은 JWT 토큰 사용
   - LocalStorage에 저장 권장

2. **API 라우팅**: 모든 요청은 API Gateway(:8080)로 전송
   - 예: `POST http://localhost:8080/api/v1/schedules`

3. **E-Campus 캘린더**: 읽기 전용
   - Canvas 과제는 자동 동기화로만 추가됨
   - 사용자가 직접 일정을 추가할 수 없음 (spec.md 1.1, 2.3 참고)

4. **에러 처리**: 백엔드는 표준 HTTP 상태 코드 사용
   - `401 Unauthorized`: JWT 토큰 없음/만료
   - `403 Forbidden`: 권한 없음
   - `404 Not Found`: 리소스 없음
   - `400 Bad Request`: 잘못된 요청 형식

### 백엔드 문서 동기화

프론트엔드와 백엔드 간 API 명세가 변경된 경우:
1. 백엔드 Swagger 문서 확인: http://localhost:8080/swagger-ui.html
2. 상세 설계 문서 확인: [../UniSync/docs/design/api-documentation.md](../UniSync/docs/design/api-documentation.md)
3. 변경사항이 spec.md의 개념과 충돌하는 경우 팀과 논의 필요